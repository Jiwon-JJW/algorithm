package step7;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class FindByAlphabet10809 {
    public static void main(String[] args) throws IOException {
        BufferedReader bufferedReader = new BufferedReader(new InputStreamReader(System.in));

        String line = bufferedReader.readLine();

        System.out.print(line.indexOf("a") + " "
                + line.indexOf("b") + " "
                + line.indexOf("c") + " "
                + line.indexOf("d") + " "
                + line.indexOf("e") + " "
                + line.indexOf("f") + " "
                + line.indexOf("g") + " "
                + line.indexOf("h") + " "
                + line.indexOf("i") + " "
                + line.indexOf("j") + " "
                + line.indexOf("k") + " "
                + line.indexOf("l") + " "
                + line.indexOf("m") + " "
                + line.indexOf("n") + " "
                + line.indexOf("o") + " "
                + line.indexOf("p") + " "
                + line.indexOf("q") + " "
                + line.indexOf("r") + " "
                + line.indexOf("s") + " "
                + line.indexOf("t") + " "
                + line.indexOf("u") + " "
                + line.indexOf("v") + " "
                + line.indexOf("w") + " "
                + line.indexOf("x") + " "
                + line.indexOf("y") + " "
                + line.indexOf("z")
        );

    }
}
